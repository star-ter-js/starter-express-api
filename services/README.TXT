this directory contains javascrit file for services that retrive some data from some input

for example, a service connects to a database, retrive some data, do stuff with data and returns some output
connect to an existing webservices, retrive some data, do stuff with data and returns some output
